# k8s/customer-db.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-db-pvx
  labels: { app: customer-db-pvx }
spec:
  replicas: 1
  selector: { matchLabels: { app: customer-db-pvx } }
  template:
    metadata: { labels: { app: customer-db-pvx } }
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports: [ { containerPort: 5432 } ]
        env:
        - name: POSTGRES_DB
          valueFrom: { configMapKeyRef: { name: shopstack-config-pvx, key: CUSTOMERS_DB_NAME } }
        - name: POSTGRES_USER
          valueFrom: { secretKeyRef: { name: shopstack-secrets-pvx, key: POSTGRES_USER } }
        - name: POSTGRES_PASSWORD
          valueFrom: { secretKeyRef: { name: shopstack-secrets-pvx, key: POSTGRES_PASSWORD } }
---
apiVersion: v1
kind: Service
metadata:
  name: customer-db-svc-pvx
  labels: { app: customer-db-pvx }
spec:
  selector: { app: customer-db-pvx }
  ports: [ { protocol: TCP, port: 5432, targetPort: 5432 } ]
  type: ClusterIP
